export const shape = {
  borderRadius: 12,
};

// Valores consistentes de borderRadius para toda la app
export const borderRadius = {
  none: 0,
  sm: 6,
  md: 8,
  lg: 12,
  xl: 16,
  '2xl': 20,
  full: 9999,
} as const;